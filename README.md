# Flutter Project

This project was exported from GlideX.

## Project Structure

```
project/
├── main.dart          # App entry point
├── routes/            # App routes
│   └── routes.dart    # Route definitions
└── screens/           # All screen widgets
    ├── intro_screen.dart (Home)
    ├── auth_screen.dart
    ├── home_screen.dart
    ├── recipe_details_screen.dart
    ├── categories_details_screeen.dart
    ├── lookup_data_screen.dart
```

## Screens

- **IntroScreen** 🏠
  - Route: `IntroScreen.routeName`

- **AuthScreen**
  - Route: `AuthScreen.routeName`

- **HomeScreen**
  - Route: `HomeScreen.routeName`

- **RecipeDetailsScreen**
  - Route: `RecipeDetailsScreen.routeName`

- **CategoriesDetailsScreeen**
  - Route: `CategoriesDetailsScreeen.routeName`

- **LookupDataScreen**
  - Route: `LookupDataScreen.routeName`

## How to Use

1. Copy these files into your Flutter project's `lib/` directory
2. Make sure you have Flutter installed
3. Run `flutter pub get` to install dependencies
4. Run `flutter run` to start the app

## Navigation

Navigate between screens using:

```dart
Navigator.pushNamed(context, ScreenName.routeName);
```

All routes are defined in `routes/routes.dart` for easy management.

---
Generated by GlideX
